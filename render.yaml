services:
  - type: web
    name: readme-generator
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: GEMINI_API_KEY
        sync: false   # paste your key in Render dashboard — users never see it